function s(w1, w2) {
    return w1.concat(w2);
}

var result = s('code', 'your');
console.log(result);

// Answer these questions:
// - What does this program do? Run it and see, or Google some of the methods used ot understand them.
//  concatenate two strings together 
// - How many parameters does the function take? two
// - What is the function name?s
// - Where is the function called? with what parameters?s , 2 parameters strings
// - What does the function return? one string
// - How can this function be improved? we can remove it  and just do the + operator  .